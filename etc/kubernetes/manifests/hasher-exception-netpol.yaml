apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hasher-exception-netpol
spec:
  ingress: 
    -
      from: 
        -
          namespaceSelector:
            matchLabels:
              name: dca-phpifo
        -
          podSelector:
            matchLabels:
              app: dca-phpinfo
      ports:
        -
          port: 8080
          protocol: TCP
  podSelector: 
    matchLabels: 
      app: hasher-label
  policyTypes:
    - Ingress

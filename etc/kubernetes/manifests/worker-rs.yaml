apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: worker-rs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker-label
  template:
    metadata:
      labels:
        app: worker-label
    spec:
      containers:
        -
          image: mjgrppr/mj-dockercoins:latest-worker
          name: worker-container
          
